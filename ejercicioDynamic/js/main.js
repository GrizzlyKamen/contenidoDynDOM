const template = "<h1>Hola, Mundo!</h1>";

 //Agregando la propiedad id = 'body' al elemento <body> en el HTML
const body = document.getElementById('body');
body.innerText = template;
body.innerHTML = template;
